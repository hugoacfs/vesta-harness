# Pilot: mount the staging harness under the tailnet /harness-staging sub-path
# (reverse-proxy strips the prefix; frontend-static emits <base href>). Remove
# this drop-in + the nginx /harness-staging block to revert. Added 2026-09-10.
[Service]
Environment=DSH_BASE_PATH=/harness-staging
