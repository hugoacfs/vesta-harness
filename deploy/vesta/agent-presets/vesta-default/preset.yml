name: Vesta Default
description: Lean single-agent default (Qwen 27B). No subagents/workflows; web search via the on-box search MCP (mcp__search__*), not DeepSeek. Switch to `vesta-orch` for delegation/fan-out.
