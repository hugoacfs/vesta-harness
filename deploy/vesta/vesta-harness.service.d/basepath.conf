# Mount the production harness under the tailnet /harness sub-path (reverse-proxy
# strips the prefix; frontend-static emits <base href>). Remove this drop-in +
# the nginx /harness block to revert to the :8790 root listener. Added 2026-09-10.
[Service]
Environment=DSH_BASE_PATH=/harness
