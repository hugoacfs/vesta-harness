# One-shot CLI sibling of the `vesta` profile (same base + Vesta bundle, no web
# server). Nothing to patch since the voice modality left the fork (2026-09-24);
# the client rows have inert node halves and need no change.
[]
