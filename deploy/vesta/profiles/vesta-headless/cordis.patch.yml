# One-shot CLI sibling of the `vesta` profile (same base + Vesta bundle, no web
# server). The voice bridge is a web-host plugin (it injects webServer,
# connection, and sessionController), so it stays disabled here; the client
# rows have inert node halves and need no change.
- id: vesta-voice
  disabled: true
