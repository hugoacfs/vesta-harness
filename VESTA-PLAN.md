# Vesta Harness — plan and status log

Living document: agents working on this fork update the status table and the log as they go. Decisions live in [`VESTA.md`](VESTA.md); ops in [`deploy/vesta/README.md`](deploy/vesta/README.md).

## Status

| Phase | Goal | State |
|---|---|---|
| 0 | Fork online on vesta, styled, text-only: `vesta` profile, ember theme, Vesta brand, MCP rows; side-by-side on `:3081` / serve `:8791` | done 2026-09-05 (Landlock sandbox binary pending `musl-tools`) |
| A | Voice into the session: host bridge plugin, mic + call HUD, Python agent bridge (feature-flagged) | done 2026-09-05 (user confirmations of barge-in / typed follow-up / tone toggle still open) |
| B | Autonomy and approvals by voice | done 2026-09-05 (scripted verification; user confirmation pending) |
| C | Cutover `:8790`, retire the standalone `:8480` voice UI, move voice service sources into `services/`, upstream sync drill | done 2026-09-05 |
| P0 | Upstream reconciliation trial (verdict CLEAN/MESSY, conflict inventory) | done 2026-09-11 — **CLEAN** (see the log) |
| P1 | Upstream sync (only if P0 says CLEAN) | done 2026-09-11 (merge `08ccc0210a`, production on `b3de92969b`, tag `vesta-stable-2026-09-11-sync`) |
| P2 | Welcome notice removal + Telegram notify | done 2026-09-11 (`9325e7fcf9`, `3e11fb1037`; tag `vesta-stable-2026-09-11-p2`) |
| P3 | Voice: TTS channel limit, staging dispatch, speed chip, phone HUD + PWA, brand finish | done 2026-09-11 — 3.1 `0324095f2c`, 3.2 `1e92f31283`, 3.3 + 3.4 `a3c0181b42` + `1adfd3aaf9`; tag `vesta-stable-2026-09-11-p3` |
| P4 | Session management: archived section, restore, hard delete (confirm + export), export | planned — after P2/P3 (the sync went first) |
| P5 | Image reading MCP on CPU | planned |
| P7 | Upstream sync last (only if P0 says MESSY) | planned |

Upstream base: `deepseek-ai/deepseek-harness@d347e70` (`dsh-v0.1.3-alpha.1`), forked 2026-09-04.

## Target architecture

You open any session in Vesta Harness, press the mic, and talk. What you say lands in that same session as a normal user turn; Qwen 27B does the agentic work with the session's tools (bash, fs, search, MCP memory/search, subagents) at the session's autonomy level; you see the tool cards on screen and hear the answer (Kyutai "calm" @1.2). Text and voice interleave in one transcript. Approvals can be answered on screen or by voice.

```
Browser (Vesta Harness UI) ──mic──▶ LiveKit SFU (:8481) ──▶ livekit-agent (Python: VAD/turn, SenseVoice STT+emotion, Kyutai TTS)  [3060 only]
        ▲  fetch /api/vesta/voice/token                                        │  each room's job dials ws://127.0.0.1:3081/vesta/voice/bridge
        │                                                                      ▼  (agent on host networking; bearer = LiveKit secret)
Vesta Harness host  `dsh --profile vesta`  (127.0.0.1:3081, serve :8791 → later :8790)
  ├─ dsh-vesta-voice (host): room ↔ session binding, submits the transcript into the session,
  │    streams assistant text back to be spoken, relays tool status, "spoken mode" prompt section
  ├─ dsh-client-ui-vesta-voice: mic button + call HUD (orb, state, mute, hide-emotions, end)
  ├─ dsh-client-ui-vesta-theme / -brand: ember tokens, fonts, ambient ground, Vesta mark, hero orb
  └─ everything else is stock DSH: Qwen via LiteLLM, tools, presets, permission tiers, sessions
```

Standing constraints: voice models run on the RTX 3060 only (`GPU-4c4e6e17-…`), never the 3090 (Qwen vLLM); Qwen via LiteLLM `default` with thinking disabled; tailnet-only exposure, ports on loopback / `192.168.0.2` / `100.120.132.105`, never `0.0.0.0`; secrets only in chmod-600 files, never in git or logs.

## Phase 0 — fork online, styled

1. Clone `hugoacfs/vesta-harness` locally and on vesta; `upstream` remote; branch `vesta`. ✅ 2026-09-04
2. Scaffold `dsh-vesta-app`, `dsh-client-ui-vesta-theme`, `dsh-client-ui-vesta-brand`, fonts, `deploy/vesta`, aggregate references. ✅ 2026-09-04
3. Build on vesta (`pnpm install`, `pnpm run build`). ✅ 2026-09-05 — `musl-tools` + landlock native build still pending (needs sudo); until then only `danger-full-access` runs bash.
4. Home `~/.vesta-harness` (profile, settings, credentials, `vesta-orch` preset). ✅ 2026-09-05
5. `vesta-harness.service` on `:3081`; `tailscale serve --bg --https=8791 http://127.0.0.1:3081`. ✅ 2026-09-05
- Verify: `--dump-config` lists the Vesta rows; `:8791` loads with Vesta brand + ember theme; a text session answers via Qwen `default`; `mcp__memory__*` / `mcp__search__*` present; `vesta-orch` active; `/permission` shows the 3-tier table; `:8790` untouched.

## Phase A — voice into the session

1. Host plugin `packages/vesta/vesta-voice/` (`@deepseek-ai/dsh-vesta-voice`): config `livekitUrl`, `apiKeyRef`/`apiSecretRef` (credential references), `bridgePath`, `mediaUrl`, `tokenTtlSeconds`, `roomPrefix`. Routes on the authenticated `/api` channel: `GET /api/vesta/voice/token?sessionId=` (LiveKit JWT for room `dsh-<sessionId>`), `GET/POST /api/vesta/voice/emotion` (proxy to the STT sidecar's `/config`). Bridge: `ctx.webServer.registerUpgrade('/vesta/voice/bridge')`; each agent job dials it with `Authorization: Bearer <LiveKit secret>` and `X-Vesta-Room`; the Host resolves/resumes the Session's Agent (`ctx.sessionController.resolveAgent`), registers the spoken-mode section on `agent.ctx.systemPrompt`, relays `agent/assistant-stream` `text-delta` chunks as `speak`, `tool/call` as `status`, `turn/end` as `done`; `turn` → `sessionController.prompt` (`queue` when idle, `steer` when running); `interrupt` → `sessionController.cancel` (inbox kept). ✅ written 2026-09-05
2. Client plugin `packages/client/ui-vesta-voice/`: mic button (`conversation.input.right`, order 40) and call HUD (`conversation.input.overlay`, order 2) over one store; `VoiceCallController` owns the LiveKit `Room` (token fetch, mic, agent audio, `lk.agent.state`, audio level → orb, perception toggle). `livekit-client` bundled privately; copy in the `vesta.voice` dictionary. ✅ written 2026-09-05
3. Agent worker moved into the fork (`services/livekit-agent/agent.py`): `DSH_BRIDGE_URL` set + room `dsh-*` → `DshBridgeLLM` (a LiveKit `LLM` whose stream is the bridge's `speak` deltas, with `SpokenTextFilter` dropping fenced code from speech and stripping inline markdown); otherwise direct Qwen-via-LiteLLM as before (the standalone `:8480` UI keeps working). Barge-in cancels the LiveKit stream → `interrupt`. Compose (`deploy/vesta/livekit-voice.docker-compose.yml`): agent on `network_mode: host`, build context = the checkout, STT sidecar published `127.0.0.1:8011`. ✅ written 2026-09-05
- Verify (server side, 2026-09-05): deployed on vesta — harness rebuilt with the plugin (bridge route answers 401 to a bad bearer), agent image rebuilt from the checkout on host networking and registered with the SFU, STT sidecar on `127.0.0.1:8011`. `deploy/vesta/bin/vesta-bridge-check` run inside the agent container against a real session: `ready` → `turn` → reply streamed as `speak` frames → `done` ("Hello there! I've got a memory set that reads, writes, searches… plus a search set…"), ~2.7 s wall for a one-liner. ✅
- First user test (2026-09-05 12:13 UTC) failed: the call HUD was registered in `conversation.input.overlay`, whose anchor is a zero-height absolute box at the composer card's top edge, so the HUD flowed over the text input ("UI quite broken"); the user re-clicked three times in 15 s, and the agent session had closed on the first participant's disconnect (`close_on_disconnect` default), leaving the room agentless. Fixes: HUD positioned absolutely above the card (as the slash menu does), `RoomInputOptions(close_on_disconnect=False)`, a failed start now leaves the room, `BRIDGE_GREETING` spoken cue. The browser did publish a microphone track on every join, so mic permission and the SFU path were fine.
- Verify (synthetic caller, 2026-09-05): `deploy/vesta/bin/vesta-call-check` run inside the agent container (joins the room as a STANDARD participant, plays a Kyutai-synthesized question, reports `lk.agent.state` transitions and audio received back). Result: STT transcribed the played question exactly ("Hello, Vesta, what is 2 plus 2 answer in a few words."), agent `thinking → speaking → listening`, 51 s of agent audio received (cue + answer), bridge bound. HUD verified rendered above the composer (screenshot via the tunnel with a stubbed microphone). Two-round probe: first reply ~15 s after the question ended (binding the call registers the spoken-mode section, so the first turn pays a full prompt prefill), second turn **3.9 s** (STT → harness → Qwen → first Kyutai audio). ✅
- Latency levers (2026-09-05): (a) ✅ the spoken-mode guidance is now a per-turn injected note (`VOICE_TURN_NOTE`), so call start/end no longer changes the request prefix; (b) ✅ the `vesta` provider declares `reasoning: medium` + `default` model `reasoningEfforts {off, medium}` + `compat.thinkingFormat: qwen-chat-template`, and the bridge selects `off` while a call is bound (pi-ai sends `enable_thinking=false`), restoring the effort on unbind; (c) `BRIDGE_GREETING` masks part of the first-turn wait. Measurement below.
- User test 2 (2026-09-05): "working okay" — the transcript shows spoken turns (workspace listing, a `pwd` tool call) answered by voice. Follow-up: the floating HUD hid the last lines of the transcript → moved from `conversation.input.overlay` to `conversation.input.dock` (in-flow above the composer, goal-bar width); verified with a screenshot that the transcript stays fully visible above the bar. ✅
- Still to confirm by the user: barge-in cancels; typed follow-up mid-call; the tone toggle.

## Phase B — autonomy and approvals by voice

1. Spoken approvals: while a room is bound, the bridge is the outermost `approval/request` answerer for that Agent (`prepend`, because the on-screen answerer holds the chain until a click). It sends `approval {id, tool, reason}` to the agent job, re-dispatches the rest of the chain with a request whose cancellation it owns, and races the two. Spoken `approval-decision` → `allowed-once` / `rejected` and the derived request is aborted, which withdraws the on-screen card through the gateway; an on-screen decision or the asker's cancellation → `approval-done` so the agent stops asking; a chain answering `unavailable` (no browser) leaves the spoken answer as the only channel. Agent side: the question is spoken by the open LLM stream (which then ends) or by `session.say` when no stream is open; the next utterance is judged yes/no by a small classifier (perception note stripped), an unclear answer is re-asked, and the assistant's continuation after the decision is spoken unprompted (`session.say` over a streaming iterator) since it arrives with no stream open. ✅ written 2026-09-05
2. Voice commands, handled in the agent worker before anything reaches the model: "stop" / "cancel that" / "never mind" → `interrupt`; "switch to safe|read-only / workspace / full access|dangerous mode" (needs a verb and a mode word) → `permission {preset}` → the host applies it through `permissionPresets.set` (logged as `permission/preset`, header updates) and confirms with `say`. `ready` now carries the preset in effect. ✅ written 2026-09-05
3. `vesta-voice` agent preset: **not built**. The per-turn spoken-mode note (Phase A) already gives the persona, and a preset with fewer tools would only buy first-turn prefill (≈8.4k of the ≈10k prompt tokens are the 36 tool schemas) at the price of capability; revisit if first-turn latency matters more than parity with typed sessions.
- Verify (2026-09-05, scripted caller in the agent container against a browser-created session, browser watching through the tunnel): "Switch to safe mode." → "Switched to read-only mode." 3.0 s after the utterance, composer selector flips to Read Only, log `permission/preset read-only` + `sandbox/mode` + `approval/policy ask`. "Please create a file called hello.txt in the workspace containing the word hi." → the model's `write` fails closed in read-only, it retries with `sandbox_permissions: danger-full-access`, `approval/asked` lands, the on-screen card shows the escalation reason and the same question is spoken 11.2 s after the utterance. "Yes, go ahead." → "Okay, going ahead." 2.6 s, `approval/decided allowed-once`, the card disappears, the file is written, and the assistant's closing sentence is spoken unprompted 4 s later (passive speech). ✅

## Phase C — cutover and consolidation

1. serve `:8790` → `127.0.0.1:3081`, `dsh-web` disabled (installed; `~/.dsh` untouched and still the only reader of the pre-cutover sessions), `:8791` off, nginx `/dsh` redirect unchanged (it targets `:8790`). ✅ 2026-09-05
2. Standalone `:8480` voice UI retired: `livekit-webui` and `livekit-frontend` behind the compose profile `legacy` (containers stopped, images kept), serve `:8480` off; `:8481` SFU stays. ✅ 2026-09-05
3. Voice service sources consolidated: `services/livekit-agent` (Phase A) and `services/livekit-media` (SenseVoice STT sidecar, moved from the compose dir) with the compose `build:` contexts pointing at the checkout; `/srv/ai/compose/livekit-voice/{PLAN,README}.md` carry a pointer here. ✅ 2026-09-05
4. Upstream sync drill: `git fetch upstream` → `upstream/master` is still `d347e70` (`dsh-v0.1.3-alpha.1`, our base), `master --ff-only` and `vesta ← master` both "Already up to date"; the procedure in `VESTA.md` ran without conflicts because nothing new exists upstream yet. The first real sync will exercise the three known conflict points (aggregate tsconfig refs, `apps/cli/package.json`, `tsconfig.base.json` paths). ✅ 2026-09-05

## Rollback

rc.7: `systemctl --user enable --now dsh-web && tailscale serve --bg --https=8790 http://127.0.0.1:3080` (the new harness stays up on `:3081`). Standalone voice UI: `docker compose --profile legacy up -d livekit-webui` + `tailscale serve --bg --https=8480 http://127.0.0.1:3010`. The agent bridge is env-flagged (`DSH_BRIDGE_URL`). Nothing is deleted; `~/.dsh` is never written.

## Plan 2026-09-11 — feature phases (from the interview)

Status: approved 2026-09-11, work started the same day; each phase ends with a documentation break (this file, the runbook, `~/vesta-docs`). Baseline: tag `vesta-stable-2026-09-11` (production and staging on `10251477ea`).

### Decisions recorded (interview)

| Question | Decision |
|---|---|
| Upstream sync placement | **Trial decides**: a time-boxed reconciliation trial first; clean → sync as Phase 1, messy → sync as the last phase |
| Permanent delete | **Hard delete with confirmation + automatic export** to `~/backups/sessions-deleted/` before removal |
| First batch | **Welcome notice removal + Telegram notify** |
| Voice, after the defects | **Speed chip**, **phone-friendly HUD** (+ PWA under `/harness/`) |
| Session management timing | **Trial decides** (after the sync if it goes first; otherwise before it, re-fitted after) |
| Extras in this plan | **Image reading MCP (CPU)**, **finish the brand pass** |
| Not in this plan | Landlock tiers, server upgrade runbook (separate track), live words in the call bar, barge-in confirmation, voice-session titles, GitHub Actions off, memory MCP review (backlog) |

### Standing rules for every phase

- Staging first (`~/code/vesta-harness-staging`, branch `staging`, `/harness-staging/`), then merge into `vesta`, then the production update. Commits on `staging`, pushed; never hand-copied files.
- A change under `packages/client/*` is live only after `pnpm run build` **and** the unit restart.
- Every production promotion ends with a tag `vesta-stable-<date>` and entries in `VESTA-PLAN.md`, the runbook, and `~/vesta-docs` when the server changes.
- Upstream packages are not edited except the recorded exceptions in `VESTA.md`; new work goes into Vesta packages (`packages/vesta/*`, `packages/client/ui-vesta-*`) and the `vesta-app` bundle.
- Voice models only on the RTX 3060; nothing new on the 3090; binds only loopback/LAN/tailnet; secrets only in `.env` / `.credentials.yaml`.
- The paused server-upgrade runbook runs as its own track; no upgrade phase during a promotion, and its "harness sync" phase is superseded by Phase 0/1/7 here.

### Phase 0 — Upstream reconciliation trial (time box: half a day)

Goal: a verdict, CLEAN or MESSY, plus a conflict inventory. Upstream is `upstream/master` at `c291e7961a` (rc.2 + 139), 1301 commits past the fork base `d347e70390`.

Facts measured 2026-09-11: the fork patches 17 upstream files — the Firefox lossless-JSON check (`packages/util/values` + test), the six base-path files (`gateway/stream-client.ts`, `connection/browser-auth.ts`, `connection/client/rpc.ts`, `file-upload/client/runtime.ts`, `hmr/client/index.ts`, `modules/index.ts`, `host/frontend-static/index.ts`), `apps/cli/package.json` (dep), and the brand assets under `apps/web` (index title, favicon, manifest, three fonts). **None of these changed upstream since the base**, so textual conflicts should be limited to tsconfig aggregates, generated paths and manifests. The risk is semantic: moved UI slots, a changed session codec, re-derived presets.

0.1 Doc fix first: add the six base-path files to the "Fork patches to upstream packages" table in `VESTA.md` (they are unrecorded exceptions to D2), with retire condition "upstream serves under a base path natively". Commit on `vesta`.
0.2 Trial merge in a scratch worktree on the Mac (no production risk): `git worktree add ../vesta-merge-trial -b merge-trial vesta && git merge upstream/master`. Record conflicts by directory; resolve the mechanical ones (tsconfig refs for our packages, `apps/cli/package.json`, `pnpm run gen-tsconfig-paths`).
0.3 `pnpm install && pnpm run typecheck && pnpm run build`; fix bundle rows whose ids or slots moved (`conversation.input.right`, `conversation.input.dock`, the brand slot, `connection` row) and our presets, which copy upstream's `standard` composition (re-derive `vesta-default`/`vesta-orch`/`vesta-voice` from the new `standard`).
0.4 Boot the merged tree on staging: push `merge-trial`, check it out in the staging checkout, build, restart. Checks: existing sessions open (session codec compatibility, the single biggest risk), typed turn through `default`, MCP tools present once, brand/theme/HUD render (headless Firefox screenshot desktop + 390 px), `/harness-staging/` base path still works, the Firefox patch still needed (upstream may have fixed it), settings/presets load without `UNSUPPORTED_*` errors. Voice on staging stays parked (named worker); the bridge route answering 401 is enough here.
0.5 Verdict rule: CLEAN = builds, sessions open, HUD/brand render, and the remaining fixes are ≤ 1 day; otherwise MESSY. Write the verdict and inventory into `VESTA-PLAN.md`; restore staging to `origin/vesta` afterwards.

### Phase 1 — Upstream sync (only if CLEAN; otherwise this becomes Phase 7)

1.1 Back up `~/.vesta-harness/sessions` and `~/.vesta-harness-staging/sessions` (tar to `~/backups`): sessions written by the new version may not open in the old one, so the rollback needs them.
1.2 Per `VESTA.md`: `git checkout master && git merge --ff-only upstream/master && git push origin master`; `git checkout vesta && git merge master` using the Phase 0 resolutions; re-apply or retire each patch in the fork-patch table; presets re-derived; `pnpm install && pnpm run gen-tsconfig-paths && pnpm run build`.
1.3 Staging: build, restart, the Phase 0.4 checklist again plus a scripted RPC turn.
1.4 Production: build, restart, then a scripted bridged call and one real call; tag `vesta-stable-<date>`; `VESTA-PLAN.md` records the merged upstream SHA; runbook and `~/vesta-docs` updated where behaviour changed (new sidebar, files panel, settings page).
Rollback: `git reset --hard vesta-stable-2026-09-11`, rebuild, restart, restore the sessions tar if the new codec wrote anything.
Effort: 1–2 days.

### Phase 2 — First batch: welcome notice + Telegram notify

2.1 Welcome notice. Mechanism (read 2026-09-11): the dialog lives in upstream's `ui-settings-models`; it is acknowledged by writing `ui-onboarding.welcomeNoticeVersion = 2026-08-13.1` into a settings scope, and for a remote browser that scope runs in memory mode ("advance only this process for a remote browser"), so the acknowledgement never persists and every tailnet visit sees the dialog. Options in order of preference: (a) a settings-scope option that persists for remote browsers, if one exists; (b) a Vesta client plugin that acknowledges the store at boot, if the store or a slot is reachable; (c) a small recorded patch to `ui-settings-models` gating the dialog behind a config flag (`welcomeNotice: false`), listed in the fork-patch table with retire condition "upstream drops the notice" (rc.2 still has it). Verify: a fresh headless-Firefox profile lands on the app with no dialog; the "Continue" path still works when the flag is on.
2.2 Telegram notify: new host plugin `packages/vesta/vesta-notify` that observes session events the way `vesta-voice`'s bridge does (passively; it answers nothing). Triggers: (i) a turn that ran ≥ `minTurnSeconds` (default 120) finished while no client stream is attached to that session (gateway stream-mux subscriber count, else a last-activity heuristic); (ii) an approval or ask-user question unanswered for ≥ `approvalWaitSeconds` (default 60). Message: session title, the trigger, the first 200 characters of the last assistant text, and the `/harness/` link. Delivery through the mounted MCP server (`http://127.0.0.1:7335/mcp`, tool `notify`), never the bot token. Rate limit one message per session per 5 minutes; `quietHours`; config on the bundle row. Verify on staging with a scripted long turn and the browser closed, and with a `read-only` session that hits an escalation approval.
2.3 Promote: staging → `vesta` → production; tag; docs (`packages/vesta/vesta-notify/README.md`, runbook, `~/vesta-docs/services/telegram-mcp.md` gets a "used by the harness" paragraph).
Effort: 1 day.

### Phase 3 — Voice: defects, then speed chip, phone HUD, brand finish

3.1 Defects. (a) TTS channel limit: moshi `batch_size = 2` for TTS; a long tool-heavy call briefly needed a third socket and LiveKit retry-stormed (`no free channels`, `retrying in 2.0s`). Fix in `services/livekit-agent/kyutai.py`: one synthesis socket per call at a time (queue the next utterance and the fillers behind it), a bounded retry (fail the utterance after 3 attempts with a log line, never a storm), and measure whether `batch_size` 3 fits the 3060 (about 2.2 GB free today) before touching `configs/vesta.toml`. (b) Staging dispatch: when `agentName` is set, the token route calls LiveKit's `AgentDispatchClient.createDispatch(room, agentName)` (server SDK) so the named staging worker gets `dshs-` rooms; production stays unnamed. Verify: scripted caller on staging with `ROOM_PREFIX=dshs-` binds and answers; a 30 s tool-heavy scripted call on production shows no `no free channels`.
3.2 Speed chip (old plan A4): a 1.0 / 1.1 / 1.2 / 1.3 chip in the call bar → host route `POST /vesta/voice/config` → bridge frame `config { speed }` → `KyutaiTTS.speed` (the pacer stretches, pitch preserved); persisted in the harness settings section `vesta-voice.speed`; default stays the env `TTS_SPEED`. Verify: a scripted call at 1.0 and 1.3 logs the applied speed and starves 0.
3.3 Phone HUD + PWA: test on your phone over the tailnet; fix the call bar, mic menu and composer at ≤ 400 px (wrap the meter/signal glyph, larger tap targets); `manifest.webmanifest` `start_url` and `scope` under `/harness/` (today `/`), icons; verify "Add to Home Screen" and the microphone permission flow on iOS Safari and Android Chrome.
3.4 Brand finish: screenshot review on staging (desktop + phone), list what the 09-10 savepoint left open (wordmark, caret, emblem, ambient ground), finish it, document in `packages/client/ui-vesta-brand/README.md`.
3.5 Promote; tag; runbook "Smooth speech"/"Staging instance" updated; `~/vesta-docs/services/voice.md`.
Effort: 2–3 days.

### Phase 4 — Session management (position set by the Phase 0 verdict)

4.1 Host plugin `packages/vesta/vesta-sessions` with RPCs: `vesta/session/unarchive` (remove the id from `archivedSessionIds` through the workspace registry, else the workspace controller's model; emit the workspace event so every client updates), `vesta/session/export` (markdown transcript + the raw `session.v2.jsonl.zstd` + attachments as a tar under `~/backups/sessions-export/`), `vesta/session/delete` (refuse a running session or one bound to a call; export to `~/backups/sessions-deleted/<id>-<date>.tar`; remove the session directory, the workspace references, the `session_projcache` entry and the search-index rows; emit the event).
4.2 Client plugin `ui-vesta-sessions`: an "Archived" section per workspace with Restore / Export / Delete; "Delete permanently…" in the session row menu; confirmation dialog showing the title and the export path; toast on completion.
4.3 Verify on staging with throwaway sessions: restore returns a session to the sidebar; delete removes it everywhere, the export tar opens, the harness stays healthy when the deleted session was open in another tab; a call-bound session is refused.
4.4 Promote; tag; runbook section "Sessions: archive, restore, delete, export"; `~/vesta-docs/services/vesta-harness.md` Sessions section.
Effort: 2 days (+ half a day of re-fit in Phase 7 if the sync goes last).

### Phase 5 — Image reading MCP (CPU only)

5.1 Compose project `/srv/ai/compose/vision-mcp`: Ollama on CPU (loopback `127.0.0.1:11434`, CPU and memory quota, short `keep_alive`, model `qwen2.5vl:3b`) plus a small MCP server exposing `read_image(image, question?)` → text. Nothing touches the GPUs.
5.2 Mount through the home patch (`deploy/vesta/home-cordis.patch.yml`, machine-dependent) as `mcp__vision__read_image`. Set the pi-ai image-offload budget so the text-only `default` lane never receives image bytes (the load-bearing setting); confirm the attachment handle the model sees is resolvable by the tool (the number one risk; the tool needs read access to the attachment store). A LiteLLM `vision` alias is optional and gets its own go/no-go.
5.3 Verify: attach a screenshot in a typed session and ask what it says; by voice, "read the file at <path>". Docs: `~/vesta-docs/services/vision-mcp.md`, README inventory row, runbook.
Effort: 1–2 days.

### Phase 7 — Upstream sync last (only if Phase 0 said MESSY)

Same steps as Phase 1, using the Phase 0 inventory, then re-fit what Phases 2–5 added: the session UI hooks (sidebar), the notify observer (event names), the onboarding suppression (if it became a patch). Verify everything from the Phase 1 checklist plus each feature's own check. Effort: 2–3 days.

### Order and effort

Phase 0 (0.5 d) → Phase 1 if CLEAN (1–2 d) → Phase 2 (1 d) → Phase 3 (2–3 d) → Phase 4 (2 d) → Phase 5 (1–2 d) → Phase 7 if MESSY (2–3 d). Roughly two working weeks of agent time, in batches small enough that each promotion is one merge, one build, one restart, one tag.

### Backlog (deliberately not scheduled)

Landlock permission tiers (needs `musl-tools` via sudo); live words in the call bar; barge-in confirmation; voice-session titles from the main model; GitHub Actions off on the fork; memory MCP review; a Vesta welcome page replacing the upstream notice.

## Log

- 2026-09-04 — Fork cloned (`d347e70`), `vesta` branch created locally and on vesta, `pnpm install` green on both. Scaffolded the three Vesta packages, the bundle, fonts, deploy tree; aggregate references added.
- 2026-09-05 — Local `pnpm run build` green (230 client artifacts). `dsh --profile vesta --dump-config` lists the Vesta rows after `dsh-web-app`. Booted locally on `:3082` against a scratch home: ember token layer applied (`--dsw-alias-bg-base: #07080c`), all three self-hosted fonts loaded, sidebar orb + "Vesta Harness" wordmark, hero orb, `vesta-orch` preset (ported, `+command-goal`) resolves as "Vesta Orchestrator"; no console errors. Note: 0.1.3 prints a tokenized startup URL; the browser exchanges it once for a persistent signed cookie (`journalctl --user -u vesta-harness` shows the URL after each start). Stock DeepSeek API-key onboarding modal shows once ("Configure later").
- 2026-09-05 — Deployed on vesta: `pnpm run build` green from the checkout, home `~/.vesta-harness` created from `deploy/vesta` (credentials copied with `install -m 600`), unit `vesta-harness` active on `127.0.0.1:3081`, `tailscale serve :8791 → 3081`. Verified through the tailnet with curl: `?token=` → 303 + signed cookie, index 200 with the boot payload listing `ui-vesta-theme` / `ui-vesta-brand`, shell asset and `/vesta/fonts/*` 200; boot journal quiet. Old install untouched (`dsh-web` active, `:3080` → 200, `:8790` unchanged). Plugin bundles are served as combos (`/plugins/??<id>,<id>…`), not per-entry paths.
- 2026-09-05 — Live deployment exercised through an SSH tunnel (browser) and the new `vesta-headless` profile (CLI): theme/brand/fonts render, hero shows "Vesta Orchestrator"; Qwen `default` answered "pong" via the `vesta` LiteLLM provider in ~14 s including boot; the agent lists all 11 MCP tools (`mcp__memory__{delete,list,read,search,status,write}`, `mcp__search__{fetch_url,news_search,search_and_fetch,search_status,web_search}`). Phase 0 verification complete; only the Landlock native build (sudo `musl-tools`) is outstanding.
- 2026-09-05 — Latency after the Phase A levers: first spoken turn 16.5 s, second 3.8 s (synthetic caller). The first-turn cost is prompt prefill after the KV cache is evicted, not thinking (reasoning is `off` during calls; the session log shows `model/selection {reasoningEffort: off}` and no `reasoning-chunks`): the request is ≈10k tokens — a 6.7k-char system prompt (≈1.7k tokens) plus 36 tool schemas (≈8.4k tokens; largest `workflow` 4.1k chars, `bash` 3.3k, `subagent_fork` 1.5k, `subagent` 1.5k, `ask_user_question` 1.4k, `todo_write` 1.4k). A leaner tool set is the only remaining lever and is a capability trade-off, so it is left to the user.
- 2026-09-05 — `vesta-headless` profile crashed at boot ("dsh-vesta-voice: pending, waiting for webServer, connection, sessionController"): the headless stack has no web server. Fixed by disabling the `vesta-voice` row in `deploy/vesta/profiles/vesta-headless/cordis.patch.yml` (applied live); the smoke check answers "pong" again.
- 2026-09-05 — Empty-session report (a voice test session shows an empty transcript when opened; console: "Assistant stream raw chunk must be a lossless JSON object"). All four stored session logs on vesta pass `expandAssistantStream`, both voice sessions render in a fresh browser, and live and barge-in turns render; not reproducible so far. Most likely a stale client tab across the harness restarts of the day (hard reload). Left open: needs the session title and, if it persists after a reload, the "Session log" ZIP from the session menu.
- 2026-09-05 — Phase B written: `types.ts` wire frames (`approval`, `approval-done`, `say`, `approval-decision`, `permission`, `ready.permission`), `bridge.ts` answerer + permission switch (`dsh-user-approval`, `dsh-permission-presets`, `dsh-scope` added as deps/refs), `agent.py` classifiers, passive speech and command handling (`DSH_COMMAND_TIMEOUT_S`), `vesta-call-check` plays a list of WAVs and settles. Approvals in stock DSH arise from sandbox escalation (`sandbox_permissions`) and hook asks only, so `danger-full-access` sessions never ask; tests run in `read-only`.
- 2026-09-05 — Phase B deployed and verified (see the Phase B verify line). Lesson: session ids are `session-<uuid>`, so bridge rooms are `dsh-session-<uuid>`; two probes with bare uuids answered 404 ("not found" from `sessionQuery.observeSession`) and cost an hour, and the headless-profile sessions in the `vesta-harness` workspace are resumable too once named correctly. Plugin logger output does not reach the journal; the runbook now says so. The spoken approval question keeps the model's justification as its own sentence.
- 2026-09-05 — Phase C: STT sidecar sources moved into `services/livekit-media`, compose contexts point at the checkout, standalone UI + frontend behind `profiles: [legacy]`, `vesta-url` prints `:8790`; runbook rewritten for the cutover and rollback; sync drill a no-op (upstream unchanged since the fork). Deployed: see the next line.
- 2026-09-05 — Phase C deployed on vesta: compose installed (backup `docker-compose.yml.bak-20260905b`), `livekit-media` and `livekit-agent` rebuilt from the checkout, `livekit-webui` stopped, serve `:8480` off, serve `:8790` → `127.0.0.1:3081`, serve `:8791` off, `dsh-web` disabled and stopped (no `:3080` listener), `vesta-url` prints `:8790`. Verified from vesta: `:8790/` answers the harness's 401 auth gate (cookie or `vesta-url` token), `:8480` and `:8791` refuse, STT sidecar `/config` OK, agent registered with the SFU. Pointers added to `/srv/ai/compose/livekit-voice/{PLAN,README}.md`. Outstanding for the user: `sudo apt-get install -y musl-tools` + the Landlock native build (until then `read-only`/`workspace-write` rely on escalation approvals), disabling GitHub Actions on the fork, and confirming barge-in, typed follow-ups mid-call, the tone toggle, spoken approvals and mode switches from a real call.
- 2026-09-05 — Blank-transcript bug found and fixed: the user is on Firefox; `@deepseek-ai/dsh-util-values` `hasIntrinsicConstructor` compared native constructor source with the exact V8 string, so under SpiderMonkey every plain object failed `snapshotJsonValue`, every `assistant/stream` chunk was rejected on the client, and the session event feed died ("event feed subscriber failed … lossless JSON object"). Reproduced with a local headless Firefox 154 (puppeteer-core, BiDi) against the tunnel; patched with a whitespace-tolerant native-source match plus a cross-engine regression test (recorded in `VESTA.md` as a fork patch to an upstream package). Not a Vesta regression as such: rc.7 predates the check, so the fork was the first 0.1.3 build the user ran.
- 2026-09-05 — Old rc.7 sessions: 19 in `~/.dsh/sessions/--home-hugo-workspace-dsh-chat--` (format v0). The JSONL provider migrates v0 → v2 on open, so copying a session directory into `~/.vesta-harness/sessions/<workspace>/` is the migration path; the rc.7 install is back on `:8791` meanwhile. Three of the 19 are forks (`parentSession` + `seedLength` with own events numbered from seq 0); one such copy made the session index's persistence observation throw, which hid every later-copied session until it was removed. Forks stay in the old install for now.
- 2026-09-05 — Old sessions migrated: `deploy/vesta/bin/vesta-migrate-sessions.mjs` copied and pre-migrated the 14 non-fork rc.7 sessions (v0 → v2 through the real provider, 16–552 events each, ≤ 5.2 s) into the Vesta home; 3 forks skipped (see runbook). The web index could not do this itself: its reconcile runs inside search requests and aborted on the multi-second migration, and one fork copy made it throw and hide later sessions. Verification after restart in the next line.
- 2026-09-05 — Old sessions verified: after the restart the 15 migrated sessions were listed under "Ungrouped" (the sidebar groups by the workspace account's `sessionIds`), so `deploy/vesta/bin/vesta-attach-sessions.py` (run with the harness stopped) attached them to the dsh-chat account; a headless-Firefox pass opened each once (0 feed errors), which cached their titles ("Security Audit", "Windows SSH setup", "Inference status", …). rc.7 stays reachable on `:8791` for the 3 forks until their inherited prefix is reconstructed.
- 2026-09-05 — The 3 forked rc.7 sessions migrate after all (4099/4650/4368 events, ~30 s each through the real provider); the earlier "forks break the index" finding was the web process attempting that migration inside a search request. Migrated and attached, so all 19 rc.7 sidebar sessions are now in the new harness. rc.7's 4 subagent child sessions are refused ("subagent/descriptor uses unsupported descriptor version 2") and stay only in the old install; they are hidden from the sidebar in both. `vesta-migrate-sessions.mjs` now takes every session directory (forks and children included, `--skip-forks` to exclude) and `vesta-attach-sessions.py` attaches forks but not subagent children.
- 2026-09-05 — Reasoning level relabeled `medium` → `xhigh` (provider default and the model's declared level) so the selector states what the model actually runs at. Checked on vesta: the served template accepts `enable_thinking` and `reasoning_effort` ∈ {xhigh (default), medium, low} only as chat-template kwargs; top-level `reasoning_effort`, `thinking_budget`, and responses-style `reasoning.effort` are ignored; pi-ai's `qwen-chat-template` format sends only `enable_thinking` + `preserve_thinking`, so the level is cosmetic. Sessions that had recorded an explicit `medium` selection after a call were re-pointed.
- 2026-09-06 — Server documentation reconciled: `~/vesta-docs` (canonical, Docsify at `/docs/`) updated against the live box — new pages for the harness, the voice stack, Hermes, PDF MCP, ComfyUI; README inventory, gateway table, GPU allocation, exposure, and change history rewritten for 2026-09-02..06; `NAVIGATION.md` lists every listener; `AGENTS.md` rewritten as the agent guide; `~/workspace/dsh-chat/AGENTS.md` added so agents inside sessions know the server. Finding: LiteLLM `assistant` (Ornith 9B) has no container since the voice sidecars took the RTX 3060; `vesta-orch` subagents pinned to `vesta/assistant` fail until the user decides (repoint to `default`, smaller helper, or move it).
- 2026-09-06 — Relabel follow-through: the live `settings.yaml` also carried `agent-default-model.reasoningEffort: medium` (written by the Settings UI), changed to `xhigh`; the three sessions whose log had pinned `medium` were re-pointed through the web RPC (`POST /api/session/selectModel`, body `{type:'client-request', rpcId, method:'session/selectModel', payload:{args:{request:{sessionId, provider, model, reasoningEffort}}}}`; `session/list` takes `args:{_request:{}}`), because the adapter throws `UNSUPPORTED_REASONING_EFFORT` for a level the model no longer declares. Collateral from the headless-Firefox automation: eight sessions ended up in the workspace's `archivedSessionIds` (archived sessions are hidden from the sidebar everywhere), cleared by editing `storages/workspace.json` with the harness stopped. Lesson: drive the live UI through the RPCs, not blind text clicks.
- 2026-09-06 — Conversational-feel pass (the vision: ChatGPT-voice/Unmute feel with hands on vesta). Shipped: the Kyutai sidecar now lives in `services/kyutai-tts` with a `response_format: pcm` path that streams 24 kHz s16le while the model generates (speed through a stateful soxr resampler), and the agent consumes it so playback starts a few hundred ms into a sentence; the agent speaks "Let me check." when the model goes straight to tools and progress lines during silent tool work; endpointing 0.4 s / 3 s and interruption 0.4 s (LiveKit defaults 0.5 / 6 / 0.5); barge-in over unprompted speech cancels the harness turn; "what mode am I in" answered from the bridge's new `permission` notices; a `vesta-voice` preset (shell, files, search, jobs, web, MCP; no delegation/planning/skills/todo/ask-user) for a smaller prefix. Measurements follow in the next line.
- 2026-09-06 — Questions and plan reviews by voice: the bridge is now also the outermost `user-questions/request` answerer for a bound Agent (same race-and-withdraw pattern as approvals: `question {id, items}` → spoken answers → `question-answer`, a spoken answer set aborts the derived request so the on-screen card closes, `question-done` when the screen wins). The agent reads items one at a time ("Options: A, B, or C"; a plan review as "say approve or tell me what to change") and maps the reply to a label, an ordinal, an approval, or free text as the custom answer.
- 2026-09-06 — Conversational-feel measurements (scripted caller, streaming TTS live). TTS sidecar warm: time-to-first-audio 1.3–1.4 s and it streams while generating, so the LiveKit agent reaches the `speaking` state on the first chunk instead of after the whole first sentence renders. Lean `vesta-voice` preset roughly halves the prompt: tool schemas 8053 → 3967 tokens, system 1689 → 1194 (from `contextBreakdown`). End to end on the voice preset: first spoken turn 7.8 s (was ~16 s on vesta-orch cold), second turn 3.4 s; the "check how much free disk space" turn ran a `bash` tool call by voice and still started speaking 3.4 s after the utterance (the ack line covers the tool run). Remaining first-turn cost is prompt prefill after KV-cache eviction; the next lever is keeping the voice session's prefix warm.
- 2026-09-06 — "Output limit reached" investigated: the turn ends with `max-tokens` when one step spends the whole 16,384-token output on thinking (three post-cutover cases in the user's largest session: ~60k chars of coherent reasoning, no answer, at 38k and 15k input tokens; the same ending appears in rc.7 logs on 09-01 and 09-02). Not a regression: neither version sends a temperature, rc.7 got the template's default xhigh implicitly and the fork sends the equivalent explicitly. Fix applied: provider `thinkingBudgets` + model `compat.supportsThinkingTokenBudget: true`, so pi-ai sends vLLM's `thinking_token_budget` (12,288 at xhigh, answer reserve ≥ 4k); LiteLLM passes the parameter through (probed). Template synced to the live `xhigh` level name.
- 2026-09-06 — Budget follow-up: with the 12,288 cap the headless plan prompt stopped thinking on time but the 15k-char answer then hit the remaining 4k of the 16,384 output budget (`max-tokens` again), so `maxTokens` is now 32,768 with the `high` budget at 16,384 (thinking ≤ 16k, answer reserve ≥ 16k). LiteLLM and vLLM accept 32k/40k output caps (probed); the harness's compaction threshold is `contextWindow × 0.5` and does not reserve `maxTokens`, and 114,688 + 32,768 stays under vLLM's 150,000. Wire capture through a local sink confirmed the request carries `max_completion_tokens`, `thinking_token_budget`, and `chat_template_kwargs {enable_thinking, preserve_thinking}`.
- 2026-09-06 — Prefix warm-up on call bind (`vesta-voice` `warmupOnBind`, default on): the bridge submits a short greeting turn when a room binds, which greets the caller and, as a side effect, runs the full request prefix (system + tool schemas) through Qwen so the provider's prefix cache is hot before the first real utterance. The agent's fixed `BRIDGE_GREETING` is cleared so the caller hears one greeting. Best-effort: a refusal only forgoes the warm greeting. Measurement in the next line.
- 2026-09-06 — Why the first warm-up attempt did not help (session `11b83e9f`): the greeting turn's request was built with `reasoningEffort: xhigh` while the bridge's switch to `off` was still in flight, and Qwen's chat template renders a different system prompt when thinking is on (it adds the reasoning-effort line), so the greeting warmed a prefix the call never uses; turn 2 was cold again (5.4 s for 3 output tokens). vLLM's prefix cache itself works (85% hit ratio globally; a typed second turn in a fresh voice session hit 5824 of 6821 tokens, blocks are 832 tokens under `mamba_cache_mode=align`). Consequence worth knowing: typed turns (thinking on) and call turns (thinking off) never share a prefix, so the first turn after switching modality is always a full prefill; the warm-up greeting exists to absorb that once per call. Fixed by sequencing the switch before the greeting.
- 2026-09-06 — Warm-up verified (session `c4a7a944`, Vesta Voice preset, scripted caller waiting for the greeting): the model-generated greeting is spoken 3.6 s after the room binds, and the first real spoken turn then answers in 3.1 s (was 7.6–7.8 s cold); the second turn, which runs a `bash` tool call, 3.4 s. The cold first-turn problem is gone: the greeting absorbs the one prefill per call. Steady-state turn ≈ 3.1–3.4 s is now the number to attack (endpointing + batch STT + suffix prefill + 1.3 s TTS first-audio).
- 2026-09-06 — Steady-state turn budget (session `c4a7a944`, warm): harness LLM wall 1.0 s for a one-word answer (6857 prompt tokens, ~5.8k cached), 1.5 s to the first sentence of a tool turn; TTS first audio 1.35 s; the rest (~0.5 s) is endpointing plus batch STT. TTS knobs make no difference (`cfg_coef` 1.0/2.0 × `n_q` 16/32 all give first audio 1.34–1.36 s), so the floor is the model's text-to-audio delay stepped by the Python generator, not per-step compute; defaults restored (2.0/32). Both remaining levers, TTS first-audio and STT/endpointing, point at Kyutai's Rust `moshi-server` (streaming text in, ~0.3 s first audio; streaming STT with semantic end-of-turn), which becomes the next project.
- 2026-09-06 — Streaming voice server project started: `services/moshi-server` (Kyutai's Rust server, TTS + STT in one container on the 3060, `deploy/vesta/moshi-server.docker-compose.yaml`), `services/livekit-agent/kyutai.py` (LiveKit `KyutaiTTS`: streaming text in over the websocket, one socket per utterance, audio pushed as it arrives; `KyutaiSTT`: persistent socket, words as interim transcripts, FINAL on the model's pause head or a silence fallback, tone notes fetched from SenseVoice out of band), env switches `TTS_BACKEND`/`STT_BACKEND` (defaults still the sidecars until measured), probe `deploy/vesta/bin/vesta-moshi-check.py`. First image build failed on `candle-kernels` (`nvidia-smi` at build time); fixed with `CUDA_COMPUTE_CAP=86`.
- 2026-09-06 — moshi-server bring-up pitfalls, all fixed in `services/moshi-server`: (1) `candle-kernels` shells out to `nvidia-smi` at build time → `CUDA_COMPUTE_CAP=86`; (2) `127.0.0.1:8090` is nginx and a fresh compose bridge network had no DNS for the model pull → host networking with `--addr 127.0.0.1 --port 8092`; (3) the TTS module is Python-backed and `cargo install` outside the venv linked the system Python 3.10 while the packages live in the venv's 3.12 (`No module named 'huggingface_hub'`, then numpy's C extension failing) → compile under `uv run` with `PYO3_PYTHON` set, and at run time export the venv's `LD_LIBRARY_PATH` + `PYTHONPATH` and call `/root/.cargo/bin/moshi-server` by full path (the moshi Python package ships a script of the same name). The STT model (`stt-1b-en_fr-candle`) loads on the 3060 in the first attempt's log, so the STT half is already proven to start.
- 2026-09-06 — Streaming voice server live. `moshi-server` runs on the 3060 (~7.5 GB with one voice loaded; the Python TTS sidecar is parked behind compose profile `legacy` because both do not fit) and the agent uses `TTS_BACKEND=moshi` + `STT_BACKEND=moshi` with `turn_detection="stt"`. Probes: TTS first audio 0.29–0.48 s after the first word (was 1.35 s); STT words arrive ~0.6 s behind the audio and the pause head fires at the audio end. Two defects found and fixed on the way: the pause prediction can precede the utterance's last word by up to 0.8 s (a 0.9 s grace after the pause, or after the latest word, now finalizes), and with VAD-driven turn detection a late fragment was held as the next turn while the interrupted reply never regenerated (stalled 66 s) — STT-driven turn detection commits on the STT's own end of speech. Scripted calls on fresh Vesta Voice sessions: greeting 1.7 s after the room binds, plain turn 3.0–3.1 s, tool turn 3.1–4.8 s; raw turn time is at parity with the batch stack (its 0.35 s TTS and live words are eaten by the 0.9 s grace + endpointing) while the feel is better (speech starts on the model's first words, words show live, quick greeting). Next levers: `asr_delay_in_tokens` 6 → 3 with a shorter grace, and the ~1 s harness LLM time.
- 2026-09-06 — "It's not picking up my voice" diagnosed from the logs: the browser participant re-joined the same room nine times (each mic press mints a new identity) and LiveKit's RoomIO, having linked the first identity, ignores every other one; the agent stayed in the room hearing nothing until the 20 s departure timeout closed it, and a new room then worked. moshi-server had also closed the idle ASR socket after 120 s, and that error ended the recognition pump. Fixes: one identity per Session in the token route (`user-<uuid>`), `_follow_callers` in the agent (links whoever calls when the linked one is gone), STT keepalive silence after 20 s of no room audio and reconnection after any drop. Verified with two scripted callers 20 s apart on one Session (`linking caller probe-89720 (previous probe-89702)`, reply 1.5 s after the question).
- 2026-09-06 — Call HUD reworked in the harness's control language (goal-bar card, 28 px selector-fill controls, primitives `Tooltip`/`Menu`): mute is a split capsule whose caret lists the browser's microphones (kept in `localStorage`), a five-bar meter shows what the room hears, capture errors print in the bar, and the composer mic matches the attach control. Verified in headless Firefox with a fake microphone (screenshots: listening/speaking states, device menu, muted).
- 2026-09-06 — Recognition quality: Whisper second pass (`services/livekit-media` `/v1/audio/refine`, faster-whisper large-v3-turbo int8_float16 on the 3060, SenseVoice alongside for the tone note) re-reads each finished utterance; the streaming model keeps the turn-taking and the live words. Started at the pause prediction and re-run (debounced 0.25 s) after late words, it lands before the 0.9 s grace ends (`second pass waited 0ms`), and scripted replies now start 1.2–1.5 s after the question ends. Also found and fixed: the lead-in buffer for the tone lookup froze at the first 120 ms of the call (12 frames of 10 ms, not 12 chunks of 80 ms), so SenseVoice never heard the onset of an utterance; it now keeps a rolling 1.5 s.
- 2026-09-06 — Warm-up side effects fixed: the greeting turn went through the user prompt path, so a Session that starts with a call was titled after the hidden instruction ("You have just answered a…") and showed it as a user bubble; it is now a plugin-sourced message (rendered as an injection, skipped by the title provider). A fresh Session (no model chosen yet) skipped the reasoning switch and greeted with thinking on (9 s); the default selection is used for the switch and restored on unbind. Probe sessions created by the scripted checks (titles "You have just answered a…", "Reply with exactly the word…") were archived through `workspace/archiveSession`; the two "Plan, in detail and step" sessions in `~/code/vesta-harness` were left alone.
- 2026-09-06 — Halts mid-reply, round one. Measured first: moshi-server renders 16 s of speech in 7.1 s alone and 18 s in 8.2 s with the listener streaming, so the GPU sharing only slows the listener's steps (14 → 150 ms while a reply renders), which delays barge-in words rather than playback. Shipped on the agent: a playout pacer (0.35 s pre-roll, WSOLA pitch-preserving stretch at 1.2×, speed sliding to 1.0 when the buffered lead runs low, per-utterance log of min lead / audio starves / word gaps) and a listener gate (room audio held while Vesta speaks and the caller is silent, a local Silero VAD reopens the stream and flushes the held 1.5 s). Browser: connection quality and receiver stats (concealed audio, packets lost, jitter) in a signal glyph in the call bar. Scripted 21 s reply: starves 0, min lead 0.36 s, no listener GPU steps for the whole reply; a barge-in through the gate still transcribes. The user's next call decides whether the remaining halts are network concealment (then `setPlayoutDelay`) or something in the browser's playout.
- 2026-09-06 — Filler lines shipped (`services/livekit-agent/fillers.py`, `deploy/vesta/fillers.yaml`): 20 lines in Vesta's voice rendered once per voice at worker prewarm into a host volume and played through the reply's own audio stream (a blank first chunk opens that stream at turn start; the server utterance still opens on the first word so a blank never holds a TTS channel). Bridged scripted turn: "Hmm." 0.3 s in, "One sec." on the bash call, then the answer; clips 2, starves 0. Threshold `FILLER_THINK_AFTER_S` 1.0 s in production (0.8 would fire on almost every harness turn, whose first token takes 1.0–1.5 s).
- 2026-09-06 — Lesson: the scripted caller had been joining `dsh-<uuid>` rooms (bare uuid) since the re-join fix; the bridge refuses those (404) and the agent falls back to direct mode, so a run "works" (fixed greeting, fast Qwen answers, nothing persisted in the session log) without touching the harness. Every agent-side result stood (re-join, keepalive, second pass, pacer, gate, barge-in), but the reply timings quoted earlier today (1.2–1.5 s) were direct-mode numbers and the warm-up/title fix was only verified now: a bridged fresh session greets quietly (4.3 s of speech, no thinking) and is titled after the first spoken sentence. `vesta-call-check` now prefixes bare ids and documents the tell-tale log line.
- 2026-09-06 — Staging instance (plan Part C) live: `~/code/vesta-harness-staging` on branch `staging`, home `~/.vesta-harness-staging`, unit `vesta-harness-staging` on `127.0.0.1:3082`, tailnet `:8792`, voice worker `livekit-agent-staging` (compose profile `staging`, rooms `dshs-`, health port 8082). Dispatch by a named agent in the token was implemented (vesta-voice `agentName`, `AGENT_NAME`) and found NOT honoured by livekit-server 1.13.6 (the SFU kept looking for an unnamed worker, which briefly left production without an agent until the worker was unnamed again); isolation is by room prefix instead: each worker's `request_fnc` accepts only its prefix and the SFU re-offers a rejected job. Both workers' health servers now bind loopback (they had been on 0.0.0.0:8081, against the bind rule). Runbook section "Staging instance", `VESTA.md` D7.
- 2026-09-06 — `assistant` lane (Ornith 9B on the 3060) retired at the user's request: the container had been gone since the voice rebuild and the card has no room for it (~2.5 GiB free vs 10.9 GiB needed). LiteLLM keeps the `ornith-9b-grunt`/`assistant`/`aider` aliases listed but marked retired (they answer 500 until the lane returns; weights kept in `/srv/ai/models/Ornith-1.0-9B-GGUF`, project `/srv/ai/compose/ornith-9b-grunt-llamacpp` with a `RETIRED.md`). The harness no longer offers `assistant` in the model list and the `vesta-orch` subagents run on `default` (they had been failing against the dead lane). Applied to the production and staging homes.
- 2026-09-06 — Staging-only settings divergence: the LiteLLM `ceres` alias (Qwen3.6-35B-A3B on the Ceres box via Ollama, up only when that machine is awake; LiteLLM publishes no token limits for it) added to the staging home's `vesta` provider as `ceres` with the `default` context window (114688) and an 8k output cap, `reasoningEfforts: {off}`; untested by request and absent from production and from `deploy/vesta/settings.yaml`. The provider block is file-managed, which is why the Settings UI shows it locked.
- 2026-09-07 — rc.7 removed at the user's request (only production `:8790` and staging `:8792` remain): `dsh-web` stopped and disabled, unit file `~/.config/systemd/user/dsh-web.service` deleted, `tailscale serve --https=8791 off` (Serve now lists only `:8790` and `:8792`). Backed up first to `~/backups` (`dsh-web.service.bak-before-remove-20260907`, `dsh-home.bak-before-remove-20260907.tar.gz`, 32M gz of the `~/.dsh` home). `~/code/dsh` (315M) and `~/.dsh` (34M) left inert on disk; the fork depends on neither (no symlinks into `~/code/dsh`; both units set `DSH_HOME=~/.vesta-harness{,-staging}`). rc.7's four unmigrated subagent child sessions now survive only in the `~/.dsh` backup tarball. Docs updated: runbook, `VESTA.md` (D4 amendment + D7), `~/vesta-docs` (README/NAVIGATION/AGENTS/services).
- 2026-09-09 — (another agent) `vesta-default` preset created and made the default in both homes: a lean single agent (no delegation/workflows, DeepSeek `web` tool removed, persona points web lookups at the search MCP). Telegram Notify MCP (`ai-telegram-mcp`, loopback 7335, one send-only `notify` tool) mounted through a new machine-wide `$DSH_HOME/cordis.patch.yml`, which also re-mounted the search server as `vesta-search` (a duplicate, see 09-11). Brand pass started: lowercase `vesta harness_` wordmark with a blinking caret and one ember gradient, veiled-goddess emblem replacing the orb, favicon/manifest/tab title, ambient ground retuned (grain, 44 px grid, slow breathe; reduced motion honoured).
- 2026-09-10 — (another agent) Sub-path move: production `https://vesta.tail22b555.ts.net/harness/`, staging `/harness-staging/` behind the `reverse-proxy` nginx (prefix stripped, WebSocket upgrade), `DSH_BASE_PATH` via systemd drop-ins, Serve `:8790`/`:8792` off. Code: `frontend-static` emits `<base href>`; `connection` RPC, the gateway stream mux, HMR, file upload and the voice `call.ts` resolve against `document.baseURI`; `browser-auth` redirects to the mount path; `client-modules` prefixes bundle URLs and matches raw-or-prefixed keys; `ui-vesta-theme` rewrites the inlined font URLs (commits `a9ea2fd9fb`..`6eaee28660`, savepoint `ae2ff41f85`). Same day server-side: branded nginx error pages, docs site restyled, and a six-phase server upgrade runbook drafted (artifact “Vesta Upgrade Runbook”) whose harness phase predates this move.
- 2026-09-11 — Post-handover pass. Found and fixed: the font-URL fix (`6eaee28660`) had been committed after the last production build, so `/harness` served the pre-fix theme bundle (fonts 404 → fallbacks) — rebuilt and restarted; search was mounted twice (bundle `search` + home patch `vesta-search`; a session's catalogue carried both sets) — home-patch row removed in both homes, preset text repointed at `mcp__search__*`, one set verified in a fresh session; the staging checkout had drifted (uncommitted copies of production files, nine commits behind) and had lost its staging-only `ceres` entry — reset to `origin/vesta`, rebuilt, `ceres` restored, branch pushed; 52 MB of `*.bak-*`/`dist.bak-*` litter moved from both checkouts to `~/backups/harness-checkouts-bak-20260911/`; drop-ins, nginx blocks, home patch and `vesta-url` (now printing the `/harness/` URL) versioned under `deploy/vesta`; settings template default preset `vesta-default`. Verified under the sub-path: token exchange `303 → /harness/`, `<base href>`, fonts and bundles 200, RPC; scripted bridged call (greeting 8.8 s after join including dispatch, replies 2.0 s / 1.5 s after the question, the tool turn spoke the server time, fillers `Mm-hm.` / `Right.` / `Checking.`, audio starves 0); headless-Firefox call through the composer mic (Connecting → Listening 1.5 s → Speaking 7.5 s, greeting with thinking off, fonts loaded, no failed requests). Still owed by the user: one real call for the downlink concealment numbers (A0). The blank “chat” session the browser check bound to was archived afterwards.
- 2026-09-11 — Stable baseline. The user confirmed voice is fine after the sub-path move, so the pending downlink-concealment measurement (plan A0/A3) is closed unless the halts recur. Settings template synced to the live production home (it had drifted in comments and lacked `agent-default-model.reasoningEffort: xhigh`); the 09-06 preset backups moved out of both homes into `~/backups/harness-checkouts-bak-20260911/homes/`. Tag `vesta-stable-2026-09-11` marks this commit. Discipline from here: staging first, then merge into `vesta` and the production update; a client-package change is live only after build and restart; no upstream sync (1301 commits, `dsh-v0.1.5-rc.2`) until planned on its own.
- 2026-09-11 — `vesta-url` rewritten so it gives a correct URL every time: `vesta-url [prod|staging]` (legacy `VESTA_UNIT` kept), user-bus variables set for ssh/cron shells, token from the process's own journal line with the unit's newest line as fallback, and a live probe that only prints a URL the harness answered `303` to (a `401` names the stale-token case). Installed at `~/.local/bin/vesta-url` with a `/usr/local/bin` symlink so PATH never matters. Tested from ssh, `env -i`, and a non-login zsh.
- 2026-09-11 — Feature plan agreed through an interview (decisions in the plan section above): trial-decided upstream sync, hard delete with confirmation and export, first batch = welcome notice + Telegram notify, voice = defects then speed chip and phone HUD, image reading on CPU, brand finish; Landlock and the server upgrade stay separate. Work started with Phase 0.
- 2026-09-11 — Phase 0 reconciliation trial, verdict **CLEAN**. Upstream `c291e7961a` (dsh 0.1.5-rc.2 + 139, 1301 commits past the base `d347e70390`) merged into a worktree branch `merge-trial` (`31f5067145`): only two textual conflicts, `pnpm-lock.yaml` (upstream taken, regenerated) and `.gitignore` (union); `tsconfig.client.json`/`tsconfig.host.json` auto-merged; `gen-tsconfig-paths` regenerated the aliases. One type error: `vesta-voice` pinned `@livekit/protocol` 1.48.0 while `livekit-server-sdk` now resolves 1.51.0 (`RoomConfiguration` types collided) — pin aligned (`a632736d86`), then typecheck and build clean. Booted on staging (`/harness-staging/`, home seeded with a copy of the 77 production sessions): 401 gate, composed config shows every Vesta row (theme, brand, voice UI + host with `roomPrefix`, memory, search, Telegram), 78 sessions list over RPC, CLI smoke turn answered `pong` through `default`, brand/theme/fonts render on desktop and the new phone rail, and opening a v2 session migrated it to v3 in 1.5 s with the transcript intact (the v2 file is retained beside the v3 one; native new sessions are v3). Inventory for Phase 1: (1) the merge itself is mechanical; (2) `@livekit/protocol` pin → 1.51.0; (3) the tab title is now set at runtime by upstream's `ui-layout` from the build-time env `DSH_CLIENT_TITLE` (else "DSH Local Build") → build with `DSH_CLIENT_TITLE="vesta harness"`, no code change; (4) upstream's new `open-in-app` pair (host `open-in-app` + client `ui-open-in-app` rows in the web-app bundle) fetches `/open-in-app/apps` root-absolute and 404s under the sub-path → disable both rows in the `vesta-app` bundle patch (the desktop hand-off is unused here), no code change; (5) the new sidebar keeps workspaces collapsed until clicked and row titles come from the projection cache (rebuilt on open); the composer now insists on a workspace before a first message; (6) presets copy the old `standard` composition — compare with upstream's new `standard` during the sync; (7) the Firefox lossless-JSON patch is still needed (upstream left `packages/util/values` untouched) and merged without conflict; (8) voice runtime against the merged host APIs is verified only at promotion (staging voice is parked). Plan consequence: the sync is Phase 1, session management (P4) follows it.
- 2026-09-11 — Phase 1 upstream sync DONE. `master` fast-forwarded to `c291e7961a` (dsh 0.1.5-rc.2 + 139) and merged into `vesta` as `08ccc0210a`; the merge commit had to be created in the staging clone, because the pre-commit hook bundles client packages against the installed dependencies and the production checkout's `node_modules` could not be updated under the live process. Follow-ups: `e586227eba` (upstream's new open-in-app probe made base-relative, plus `deploy/vesta/bin/vesta-build`, which sets the build-time `DSH_CLIENT_TITLE`), `4be32f1ae5` (presets gain upstream's `present` tool row), `b3de92969b` (persona config `text` → `prefix`). Promotion: both homes' sessions tarred to `~/backups/*-home-sessions-before-sync-20260911T113316Z.tgz`; staging built and restarted 12:31 and checked (title, open-in-app 200, 78 sessions listed, a v2 session opened and migrated to v3 in 1.5 s, CLI turn with 37 tools and each MCP once); production built and restarted 12:34. Regression caught by the post-promotion check: every preset failed to mount (`dsh-persona` now requires `prefix`, our rows said `text`), so for about four minutes (12:34–12:38) new sessions and resumes of existing ones failed, until the presets were fixed in the templates and both homes (no rebuild needed). Verified on production afterwards: session creation with all three presets, a scripted bridged call (greeting, replies 1.9 s / 1.6 s after the question, tool turn, fillers, audio starves 0) and a headless-Firefox call through “New session in dsh-chat” → mic (bridge bound, HUD showing Listening with mute / device / End call, End call works). Observed under 0.1.5: sidebar workspaces start collapsed and rows show titles only once a session has been opened (projection cache); the composer asks for a workspace before the first message; the tab title reads `<session> — vesta harness`; sessions migrate v2 → v3 lazily on open with the v2 file retained; upstream added a `read_image` tool and file-delivery cards (`present`). Trial worktree and `merge-trial` branches removed. Rollback: `git reset --hard vesta-stable-2026-09-11 && deploy/vesta/bin/vesta-build && systemctl --user restart vesta-harness`, restoring the sessions tar if a v3-only session must be read by the old code.
- 2026-09-11 — Phase 2 DONE (staging first, then production on `3e11fb1037`, tag `vesta-stable-2026-09-11-p2`). (1) Welcome notice: the root cause is upstream's rule that a browser whose hostname is not loopback gets memory-only settings scopes (`ui-settings`), so from the tailnet the acknowledgement never persisted and the Settings page never saved either. Fix `9325e7fcf9`: `ui-settings` honours the build-time flag `DSH_CLIENT_SETTINGS_PERSISTENCE=host` (recorded fork patch; `vesta-build` sets it); the acknowledgement already in `settings.yaml` now counts, and a fresh browser profile lands on staging and production with no dialog. (2) Telegram notify `3e11fb1037`: host plugin `vesta-notify` (observes `turn/start`/`turn/end` on the durable log and sits as a passive front observer on the `approval/request` and `user-questions/request` waterfalls; delivers by a stateless `tools/call` to the send-only MCP on `127.0.0.1:7335`) plus the browser heartbeat `ui-vesta-presence` (`POST /api/vesta/notify/presence` every 30 s while a tab is visible). Defaults: long turn ≥ 120 s, approval/question waiting ≥ 60 s, only while away (no visible heartbeat for 90 s), cooldown 300 s per Session and trigger, quiet hours, link to `/harness/`. Verified: staging with short thresholds (home patch, also in `deploy/vesta/staging-cordis.patch.yml`) — away flips after the threshold, a 25 s turn delivered in 34 s; production with the defaults — a 130 s unattended turn delivered 138 s after the prompt, and a visible tab keeps it quiet. Lessons: a new client package needs its own `tsdown.config.ts` (`clientBundle(name, ['lib/types/index.js'])`) or the client pass skips it and the host refuses to boot with `client bundle not found`; the first typecheck of a new client package needs `tsc -b tsconfig.client.json` once before the host-face bundler runs; client `process.env` names must start with `DSH_CLIENT_`. Hygiene: the notifier container logged the bot token inside the Telegram API URL (httpx INFO) — its `httpx`/`httpcore` loggers are WARNING since 2026-09-11 (backup `app/server.py.bak-before-httpx-quiet-20260911`, image rebuilt); the token had been in `docker logs` and is worth rotating.
- 2026-09-11 — Phase 3.1 DONE (voice defects; `0324095f2c`, production harness and worker rebuilt, tag `vesta-stable-2026-09-11-p3.1`). (1) TTS channel limit: the moshi pool has two TTS channels for the whole box, and an utterance's socket outlives its last word by the generation tail, so a call's back-to-back replies (or a filler-opened stream plus the next reply) overlapped and the third socket was refused — that was the 2026-09-07 retry storm. `KyutaiTTS` now serialises its utterances through a per-loop gate (`_channel_gate`), an interrupted stream cancels its utterance task so the socket closes at once (barge-in no longer waits for the abandoned utterance to finish generating), a pool refusal is classified retryable (LiveKit's bounded 3 × 2 s) and every other server error non-retryable. (2) Staging dispatch: with `agentName` set the token route now also creates an explicit AgentDispatch through the SFU's API (`livekitApiUrl`, idempotent per room via `listDispatch`, best effort); the staging home patch names `vesta-staging` (`agentName` + `livekitApiUrl: http://192.168.0.2:7880`), `vesta-call-check` does the same with `AGENT_NAME`, production stays unnamed on automatic dispatch. Verified on staging: scripted call (dispatch id in the worker log, bridge bound, replies 2.0 s / 1.6 s) and a browser call through the mic; on production after the rollout: a four-question tool-heavy call (hello, news via web search, disk, uptime) answered 1.9 / 1.6 / 1.6 / 1.6 s after each question with no refusal or retry across 108 s of speech (one 35 ms starve inside the 28 s news reply, where the model paused mid-reply), and a barge-in call (over the greeting, then over the first reply) with clean state transitions and no errors. Phone check on staging: the call HUD fits a 390 px viewport (dock 268 px, no horizontal overflow); the end-call button sits just outside the bar's rounded edge — a polish item for 3.3.
- 2026-09-11 — Phase 3.2 DONE (speed chip; `1e92f31283`, production harness and worker rebuilt, tag `vesta-stable-2026-09-11-p3.2`). A capsule chip in the call bar (1.0× / 1.1× / 1.2× / 1.3×) beside the tone toggle: the choice is remembered in `localStorage` (`vesta.voice.speed`), shown at call start and pushed to `POST /api/vesta/voice/config` once the bridge is bound (a few retries while the agent job dials in), the host forwards it as a `config` frame (`VoiceBridge.configure`, 404 while no room is bound) and the agent sets `KyutaiTTS.speed`, which the playout pacer reads at its next utterance; filler clips keep their rendered speed. Verified on staging: chip at 1.2× → menu → 1.3× with 200s on both posts and the agent logging 1.20 then 1.30; a change posted while the call was idle reached the agent within a second; the same chip flow on production after the rollout. Not captured under test: the played/generated ratio of the reply after a change (three timed attempts landed before or after the reply; one early attempt showed the agent logging a mid-reply change only at teardown, not reproduced) — judge by ear on a real call, and if a mid-reply change ever lands late, the reader loop in `agent.py` `_read` is where to look.
- 2026-09-11 — Phase 3.3 + 3.4 DONE (`a3c0181b42`, `1adfd3aaf9`; production harness rebuilt; tag `vesta-stable-2026-09-11-p3`, which closes Phase 3). Phone HUD: at widths up to 480 px the call bar wraps its controls onto a second right-aligned row and hides the device name and the mic meter (a capture error still shows), so the end-call button and the speed chip stay inside the bar on a 390 px phone (measured on staging: bar 268 × 74 px, no page overflow). Home-screen install: `manifest.webmanifest` now uses `id`/`start_url`/`scope` = `./` (relative to the manifest's URL, so an installed app opens under `/harness/` instead of the landing page), `display: standalone` (was `fullscreen`, which hides the status bar and the safe areas), ember `background_color`/`theme_color` (`#07080c`), and PNG icons rendered from the emblem (180 px apple-touch, 512 px maskable) beside the SVG; `index.html` carries the `theme-color` and iOS home-screen meta tags; `frontend-static` maps `.png` (the icons had shipped as octet-stream; recorded in the fork-patch row). Brand finish: nothing left in the 09-10 savepoint beyond documentation — the emblem, lowercase wordmark and caret render on desktop and on the phone rail; `ui-vesta-brand`'s README no longer describes the orb. Still to try by hand on a phone: “Add to Home Screen” on iOS Safari and Android Chrome, and the microphone permission flow inside the installed app.
