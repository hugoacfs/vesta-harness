# livekit-* left unpinned (co-released, mutually compatible); pin from `pip freeze`
# once a build is confirmed working. aiohttp is the bridge WebSocket client
# (already a livekit-agents dependency; listed so the bridge does not rely on
# a transitive pin).
livekit-agents
livekit-plugins-openai
livekit-plugins-silero
livekit-plugins-turn-detector
httpx>=0.27
aiohttp>=3.9
